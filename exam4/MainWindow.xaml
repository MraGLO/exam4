<Window x:Class="exam4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:exam4"
        mc:Ignorable="d"
        Title="MainWindow" Height="630" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Background="Green">
            <Button Name="Back" Click="Back_Click" Style="{StaticResource BackButtonStyle}">Назад</Button>
        </Grid>
        
        <Frame Grid.Row="1" Name="MainFrame" NavigationUIVisibility="Hidden"></Frame>
        <Grid Grid.Row="2" Background="Green">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource BottomText}" Grid.Column ="0" Margin="50,0,0,0" HorizontalAlignment="Left">Кузичев М.С. ИСП-420п</TextBlock>
            <TextBlock Style="{StaticResource BottomText}" Grid.Column="1" Margin="0,0,50,0" HorizontalAlignment="Right">13.05.2024</TextBlock>
        </Grid>

    </Grid>
</Window>
